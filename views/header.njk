<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Pivot</a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            {% if user %}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item d-flex">
                        <a href="/questions" class="nav-link {% if title=='questions' %}active{% endif %}">Questions</a>
                    </li>
                    <li class="nav-item d-flex">
                        <a href="#" class="nav-link {% if title=='students' %}active{% endif %}">Students</a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item d-flex">
                        <a href="#" class="btn btn-link">
                            {{ user.name.givenName }}
                        </a>
                    </li>
                    <li class="nav-item d-flex">
                        <a href="/auth/logout" class="btn btn-primary">Log out</a>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>
<div class="row mt-3">
    {% include 'messages.njk' %}
</div>